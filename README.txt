need create DB on MySQL:
1)CREATE DATABASE `mix`;

2)CREATE TABLE `mix_users` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `username` char(100) DEFAULT NULL,
  `password` char(20) DEFAULT NULL,
  `time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);

3)CREATE TABLE `mix_users` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `username` char(100) DEFAULT NULL,
  `password` char(20) DEFAULT NULL,
  `time` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
);


